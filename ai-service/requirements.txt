# Drive Legal AI - AI Service Requirements
# Core API framework
fastapi==0.111.0
uvicorn[standard]==0.30.1
pydantic==2.7.4

# AI / CV stack (ready for real models; not required for DEMO mode)
numpy==1.26.4
opencv-python-headless==4.10.0.84
scikit-learn==1.5.0

# PyTorch and YOLO are intentionally NOT pinned here for the prototype —
# installing full torch/ultralytics is heavy and unnecessary for DEMO mode.
# To enable REAL AI MODE, uncomment and install:
# torch==2.3.1
# ultralytics==8.2.42

python-multipart==0.0.9
python-dotenv==1.0.1
